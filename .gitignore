# Artefactos generados (se reconstruyen con build_f1.py)
workflow-bot-f1.json
__pycache__/
*.pyc

# Datos con PII / binarios — no van a control de versiones
*.xlsx

# Nunca versionar secretos (por si acaso)
*token*.txt
*apikey*.txt
*.pem
.env
rollback-*.json
backups/
reportes/
workflow-bot-f1.json

# Material de oficina y personal (PII) — no versionar
oficina/
entrevista/
HV_*.pdf
*.pdf

# Artefactos que genera tests/correr.sh desde build_f1.py
tests/cerebro.js
tests/n_inactivos.js
tests/n_*.js

# Pantallazos de trabajo: no van al repo (es PÚBLICO y suelen mostrar chats de clientes)
Snipaste_*.png
*.screenshot.png
